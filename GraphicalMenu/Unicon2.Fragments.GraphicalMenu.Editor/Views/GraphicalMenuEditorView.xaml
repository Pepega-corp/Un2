<UserControl
    x:Class="Unicon2.Fragments.GraphicalMenu.Editor.Views.GraphicalMenuEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lex="http://wpflocalizeextension.codeplex.com"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="clr-namespace:Unicon2.Fragments.GraphicalMenu.Editor.ViewModel"
    d:DataContext="{d:DesignInstance viewModel:GraphicalMenuFragmentEditorViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <UniformGrid VerticalAlignment="Top" HorizontalAlignment="Left" Width="300" Columns="2">
            <TextBlock Margin="1" Text="{lex:Loc DisplayHeight}" />
            <TextBox Margin="1" Text="{Binding DisplayHeight}" />
            <TextBlock Margin="1" Text="{lex:Loc DisplayWidth}" />
            <TextBox Margin="1" Text="{Binding DisplayWidth}" />
            <TextBlock Margin="1" Text="{lex:Loc CellHeight}" />
            <TextBox Margin="1" Text="{Binding CellHeight}" />
            <TextBlock Margin="1" Text="{lex:Loc CellWidth}" />
            <TextBox Margin="1" Text="{Binding CellWidth}" />
        </UniformGrid>
    </Grid>
</UserControl>
