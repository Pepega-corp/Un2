<mah:MetroWindow
    x:Class="Unicon2.Web.Presentation.View.WebSynchronizationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lex="http://wpflocalizeextension.codeplex.com"
    xmlns:local="clr-namespace:Unicon2.Web.Presentation.View"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:Unicon2.Web.Presentation.ViewModels"
    Title="WebSynchronizationView"
    Width="800"
    Height="450"
    d:DataContext="{d:DesignInstance viewModels:WebSynchronizationViewModel}"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBlock Margin="5" Text="{lex:Loc ConnectionState}" />
            <mah:ToggleSwitch IsChecked="{Binding CanConnectToServer}" />
        </StackPanel>
        <Button
            Grid.Row="0"
            Grid.Column="1"
            Margin="5" />
    </Grid>
</mah:MetroWindow>
